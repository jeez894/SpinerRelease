using LethalLib;
using UnityEngine;

public class testfunction : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

        Debug.Log("LethalLib is active and ready!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
